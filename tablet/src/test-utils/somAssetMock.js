/**
 * `require()` de um asset devolve um numero no React Native — o id do recurso
 * empacotado. O Jest nao empacota nada, entao devolvemos um numero qualquer:
 * o que importa e o require nao explodir ao carregar a tela de resultado.
 */
module.exports = 1;
